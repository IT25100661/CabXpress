Place 240 WebP car animation frames here using this exact pattern:

frame_0001.webp
frame_0002.webp
...
frame_0240.webp

The React component `CarFrameScroller` automatically uses these files when present and shows a CSS luxury car fallback when they are missing.
