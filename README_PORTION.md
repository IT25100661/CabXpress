# Member 1 User Management

This ZIP contains the files assigned to this member for the CabXpress project.

## Main responsibility
Authentication, users, roles, OTP/email, JWT security, notifications, login/register/profile UI.

## Note
Some common/shared project files are included so the member can understand how this part connects with the full project. Heavy/generated/private folders are excluded: `node_modules`, `backend/target`, `.idea`, and `.env`.
